/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'littlesunnykitchen.com'],
  },
}

module.exports = nextConfig 